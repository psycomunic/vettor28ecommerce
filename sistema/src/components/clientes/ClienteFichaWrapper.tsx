'use client'

/**
 * ClienteFichaWrapper — não utilizado em produção (era apenas para modo demo).
 * Mantido como stub vazio para evitar erros de import em caso de referência restante.
 */
export function ClienteFichaWrapper({ id }: { id: string }) {
  return (
    <div style={{ padding: 40, textAlign: 'center', color: 'var(--lilac)' }}>
      Cliente não encontrado.
    </div>
  )
}
